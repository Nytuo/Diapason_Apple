name: Diapason
options:
  bundleIdPrefix: fr.nytuo
  deploymentTarget:
    iOS: "26.0"
    tvOS: "26.0"
    watchOS: "26.0"
packages:
  SwiftSonic:
    url: https://github.com/MathieuDubart/swiftsonic
    branch: main
  AudioStreaming:
    url: https://github.com/dimitris-c/AudioStreaming
    branch: main
  YouTubeKit:
    url: https://github.com/alexeichhorn/YouTubeKit
    from: "0.2.4"
targets:
  Diapason:
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - path: Sources
        excludes:
          - "Views/Platform/macOS/**"
          - "Info.plist"
          - "Info-tvOS.plist"
    dependencies:
      - package: SwiftSonic
      - package: AudioStreaming
      - package: YouTubeKit
      # Embeds the watch app inside the iPhone app so WatchConnectivity recognises
      # it as the installed companion (isWatchAppInstalled). Requires the watchOS
      # platform to be installed in Xcode to build the iOS target.
      - target: DiapasonWatch
        embed: true
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GENERATE_ASSET_SYMBOLS: "YES"
        ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: Diapason
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        ENABLE_PREVIEWS: "YES"
    info:
      path: Sources/Info.plist
      properties:
        UIBackgroundModes: [audio, processing]
        BGTaskSchedulerPermittedIdentifiers:
          - app.cassette.wrapped.monthly-update
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        NSLocalNetworkUsageDescription: "Diapason needs to find your Subsonic and Plex servers on your local network."
        NSBonjourServices:
          - "_subsonic._tcp"
          - "_plexmediasvr._tcp"
          - "_diapason._tcp"
        UILaunchScreen:
          UIColorName: LaunchBackground
          UIImageName: LaunchScreen
          UIImageRespectsSafeAreaInsets: true
  DiapasonTV:
    type: application
    platform: tvOS
    deploymentTarget: "26.0"
    sources:
      - path: Sources
        excludes:
          - "Views/Platform/macOS/**"
          - "Info.plist"
          - "Info-tvOS.plist"
          - "DiapasonBackends/iPod/**"
          - "DiapasonBackends/LocalImportSectionView.swift"
          - "Views/Wrapped/**"
          - "Views/Main/MainTabView.swift"
          - "Views/Main/MiniPlayerAccessoryView.swift"
          - "Views/Main/FullPlayerView.swift"
          - "Views/Main/QueueView.swift"
          - "Views/Main/EditPinnedView.swift"
      - path: TVSupport
    dependencies:
      - package: SwiftSonic
      - package: AudioStreaming
      - package: YouTubeKit
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIconTV
        ASSETCATALOG_COMPILER_GENERATE_ASSET_SYMBOLS: "YES"
        ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: Diapason
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "3"
        ENABLE_PREVIEWS: "YES"
    info:
      path: Sources/Info-tvOS.plist
      properties:
        UIBackgroundModes: [audio]
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
  DiapasonWatch:
    type: application
    platform: watchOS
    deploymentTarget: "26.0"
    sources:
      - path: WatchApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: fr.nytuo.Diapason.watchkitapp
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "4"
    info:
      path: WatchApp/Info.plist
      properties:
        CFBundleDisplayName: Diapason
        WKApplication: true
        WKCompanionAppBundleIdentifier: fr.nytuo.Diapason
schemes:
  Diapason:
    build:
      targets:
        Diapason: all
    run:
      config: Debug
  DiapasonTV:
    build:
      targets:
        DiapasonTV: all
    run:
      config: Debug
  DiapasonWatch:
    build:
      targets:
        DiapasonWatch: all
    run:
      config: Debug
